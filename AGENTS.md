## Project notes

- When committing changes that should be available through `go install github.com/oski646/repoq@latest`, create and push a new release tag after the commit is pushed.
- Use semantic versioning for release tags. New provider support should bump the minor version.
